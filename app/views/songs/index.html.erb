<h1>All Songs</h1>

<%  @songs.each do |song| %>
  <!-- <%= link_to song.title, song_path(song) %> <br> -->
  <%= link_to song.title, song %>
  <%= link_to "Edit", edit_song_path(song) %>
  <%= link_to "Destroy", song_path(song), method: :delete %>
  <br>
<% end %>

<p><%= link_to "New Song", new_song_path %></p>